# 手控器模拟时间与初始化交接

日期：2026-08-04  
执行方：Codex  
接收方：Claude Code（审核与部署）

## 完成内容

- 时间倍率扩展为 ×1、×2、×3、×5、×20、×60。
- 新增统一的 `simulatedTimestamp` 模拟时间源，按所选倍率持续推进。
- 时间模块显示模拟日期、星期和时分秒。
- 新增“初始化”按钮，一次恢复屏幕、设备数据、持久状态、触发状态、身份、氛围灯、主题、倍率和模拟时钟。
- 时间倍率同时加速手控器内部耗时操作；桌高当前按 `38 × 倍率` mm/s 模拟推进。
- 修正时间显示区的网格行定位，使标签、日期与数字时钟在模块内上下居中。

## 修改文件

- `src/stores/lab/handset.js`
- `src/components/lab/TimeSpeedControl.vue`
- `docs/lab-handset-operation-logic.md`

## Claude 待办

- 审核时间模块在 Electron 目标窗口中的视觉与宽度。
- 审核通过后部署完整 `dist-web/`；Codex 未部署。

## 验证

- `npm.cmd run build:web` 通过（Vite 5.4.21，3971 modules transformed）。
- 自动化确认六个倍率按钮完整显示。
- ×60 下等待约 1.2 秒，模拟时钟推进约 61 秒（受 200 ms 刷新周期影响，符合倍率逻辑）。
- 初始化后确认屏幕回到黑屏、倍率恢复 ×1、通知清除。
- ×2 下长按上升约 1.05 秒，高度从 100 cm 变为 108 cm，符合约 79.8 mm 的倍率位移。
- 自动化测量时间显示区与时间模块的垂直中心偏差为 0 px。
