# ECR PDM 格式 · 真实变更文件验证结论

日期：2026-08-06
审查人：Claude
被测提交：`ac9e833`（含 `3dc68a0` 的 P0/P1 返工）
样本：用户提供的**真实变更文件** `变更前.xlsx`（226 行）/ `变更后.xlsx`（227 行）
结论：**暂不部署**，1 项阻塞待修

---

## 🔴 P0 · `已停用` 不在状态词表中，两份真实文件全部被拒

```
变更前.xlsx → 第 74 行包含未知状态：已停用
变更后.xlsx → 第 38 行包含未知状态：已停用
```

`VALID_BOM_STATUSES` 目前只有 4 个值：

```python
{'已发布', '审核中', '通用变更审核中', '非通用变更审核中'}
```

真实文件里存在**第 5 个值 `已停用`**，两份各 5 行。

### 这 5 行是什么

| 编码 | 描述 | 版本 | 变更前层次 | 变更后层次 |
|---|---|---|---|---|
| `14ST01007` | 十字槽盘头尖尾自攻螺钉 | A01 | 2.4.15 / 2.4.26.3 | 3.1.4 / 3.1.15.11 |
| `14ST08001` | CPE软骨自封袋 | A01 | 2.6.5 | 3.3.11 |
| `14ST05032` | 调节地脚 | A01 | 3.2.19 / 3.5.14 | 4.3.7 / 4.5.13 |

全部是**标准件**，**前后两份文件里都存在、编码与版本完全相同**，
只是层次号因插入新物料而整体位移。即：**它们在本次变更中没有变化**，
只是这些物料本身在 ERP 里已被停用，但仍留在 BOM 中。

### 需要用户决定语义（两个选项）

1. **等同「已发布」对待**（我倾向这个）：把 `已停用` 加入 `VALID_BOM_STATUSES`，
   在版本推算与变更判定上与 `已发布` 完全一致 —— 不推算新版本、
   前后相同则判为未变更。理由：这 5 行在本次变更里确实没有变化，
   物料自身的停用状态是**物料主数据属性**，不是**BOM 变更状态**，
   两者语义不同，不该影响变更比对。
2. **在解析阶段跳过这些行**：但这样它们会从 BOM 树里消失，
   若某个已停用物料在变更中真的被删除/替换，就检测不到了。**不建议。**

⚠️ **不要保留现状（报错）**：这会让所有含已停用物料的 BOM 都无法导入，
而这类物料在真实 BOM 里很常见（本样本 226 行里就有 5 行）。

### 顺带建议

`已停用` 这个值是**真实数据带来的、词表之外的第 5 个值**。这说明状态词表可能还不完整。
建议把「未知状态」的处理从**硬报错**改为**报错信息里列出该文件出现的全部未知状态值**
（现在只报第一个遇到的），这样一次就能发现所有缺口，不用反复试。

---

## ✅ 真实数据带来的三项重要确认

### 1. 新物料规则完全成立（第一次见到真实的新物料行）

`变更后.xlsx` 层次 2：

| 字段 | 值 |
|---|---|
| 物料编码 | `14WD01093` |
| **版本** | **空** |
| **状态** | **`审核中`** |
| 描述 | 变更测试 |
| 一级/二级/三级分类 | `14_原材料` / `WD_木器` / `01_桦木` |

**与用户描述的规则完全一致**：版本空 + 状态审核中 = 新建物料。
按实现应产出品名 `原材料_木器_桦木_变更测试`、变更后图号 `14WD01093-A01`。
（本次因 P0 阻塞未能跑到这一步，修完后验证。）

### 2. 层次号会整体位移 —— 而 `compare_bom` 的设计已经能扛

插入新物料（层次 2）导致其后所有层次号位移：变更前的 `2.x` → 变更后的 `3.x`，
`3.x` → `4.x`。两份文件的层次交集极小、差异达数百行。

**幸运的是这不构成问题**：`compare_bom` 的匹配键是 `(parent_code, code)`
而非 `(level, code)`，代码注释里也写明了这个设计意图。所以层次位移**不会**
造成假增删。

> 这也说明我此前几轮用「层次」对账是可行的（那两份是同一棵树），
> 但真实变更场景下**不能**按层次匹配 —— 好在实现本来就没这么做。

### 3. `validate_bom` 的角色调用是安全的

`变更前.xlsx` 里也有 1 行 `非通用变更审核中`（层次 1 顶层 `1201TSSJ01` 探索家_书架，
前后两份文件相同）。如果变更前文件按 `role='before'` 校验（要求全部「已发布」），
它会被拒。

实际调用（`routes/rd/__init__.py:139,142`）是：

```python
err = validate_bom(before_path, role='any')    # 只校验列名，不校验状态
err = validate_bom(after_path,  role='after')  # 要求不能全是「已发布」
```

**变更前用的是 `role='any'`**，所以不会被拒 —— 这个设计正确，无需改动。

---

## 验收方式（P0 修完后我会执行）

1. 两份真实文件都能完整解析（226 / 227 行）；
2. `validate_bom` 两个角色均通过；
3. **新物料**：`14WD01093` 品名为 `原材料_木器_桦木_变更测试`、
   变更后图号 `14WD01093-A01`、在 `compare_bom` 中被判为「新增」；
4. **已停用的 5 行**：前后一致 → **不出现在变更结果里**（未变更）；
5. `非通用变更审核中` 的 `1201TSSJ01`：变更后版本按字母+1 推算（`A01` → `B01`）；
6. 完整跑一遍 `compare_bom`，人工核对变更行数量与内容是否符合业务预期
   （这一步需要用户一起看，因为只有用户知道这次实际改了什么）。
