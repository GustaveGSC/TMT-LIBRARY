# ECR/PDM BOM “已停用”发布态兼容完成

日期：2026-08-06  
对象：Claude Code

## 完成内容

### 1. “已停用”与“已发布”统一为发布态语义

- 新增 `STATUS_DISABLED = '已停用'`。
- 新增 `PUBLISHED_STATUSES = {'已发布', '已停用'}`。
- `VALID_BOM_STATUSES` 由发布态集合与三种审核中状态组合，避免只扩词表而遗漏业务判断。
- `validate_bom(role='after')` 改为检查状态是否全部属于 `PUBLISHED_STATUSES`；因此只包含“已发布/已停用”的文件仍会被正确拒绝，不会静默生成空变更单。
- 错误文案明确注明“已发布（含‘已停用’）”。
- `compare_bom()` 和 `_derive_new_drawing()` 无需修改：“已停用”不属于变更状态，会自然按未变更发布物料处理。

### 2. `_parse_bom()` 一次报告全部未知状态

- 解析业务行前先收集文件内全部非空状态。
- 未知状态按去重、排序后的列表一次性抛出，例如：`包含未知状态：待批准、草稿`。
- 不再逐行只报告遇到的第一个未知值。

## 自动化验证

- 新增“已发布 + 已停用”作为变更后文件仍被拒绝的回归测试。
- 新增前后相同的“已停用”物料不产生任何变更行的测试。
- 新增多个未知状态一次性汇总报错的测试。
- `pytest -q tests/test_rd_excel_logic.py`：11 项通过。
- 全量 `pytest -q`：295 项通过，2 项跳过。
- `python -m compileall -q backend`：通过。
- `git diff --check`：通过。

## 未执行与真实文件验收

- 未部署。
- 仓库中没有 `变更前.xlsx` / `变更后.xlsx`，因此未在 Codex 环境重复执行 226/227 行真实文件验收。
- 请 Claude Code 按审查文档的六项门禁继续验证，尤其核对：
  1. 两份文件完整解析为 226/227 行；
  2. `14WD01093` 生成 `14WD01093-A01` 并判为新增；
  3. 5 条“已停用”记录不进入变更结果；
  4. `1201TSSJ01` 按非通用规则从 A01 推算到 B01；
  5. 最终变更行数量和业务内容由用户共同确认。

本次没有接口、数据库或前端契约变更。
