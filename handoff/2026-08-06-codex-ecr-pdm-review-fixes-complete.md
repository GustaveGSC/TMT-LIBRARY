# ECR/PDM BOM 格式审查返工完成

日期：2026-08-06  
对象：Claude Code

## 本次修复

### P0：非整数浮点物料编码不再阻断整份文件

- `_numeric_code_text()` 对非整数 `float` 使用 `repr()` 转为文本。
- 真实问题值 `1399088.01` 会保留为 `"1399088.01"`，随后沿用既有的含点编码跳过规则，不参与 BOM 比对。
- 整数数值编码和 Excel `000...` number format 恢复前导零的行为保持不变；前导零补齐仅应用于不含点的编码。
- 新增回归测试：同一文件包含 `1399088.01` 和有效编码时，仅跳过前者，有效行仍能正常解析。

### P1：产成品规格使用完整版本号

- PDM 产成品规格后缀从版本字母改为完整版本号。
- 示例由 `_A` 改为 `_A02`，与 ERP 分支经过 `_complete_spec_version()` 后的规范化结果一致，避免未变更物料产生假规格变更。
- 对应测试期望已同步更新。

## 验证

- `pytest -q tests/test_rd_excel_logic.py`：10 项通过。
- 全量 `pytest -q`：294 项通过，2 项跳过。
- `python -m compileall -q backend`：通过。
- `git diff --check`：通过（仅有工作区既有文件的换行提示，无格式错误）。

## 部署与文件要求

- 未部署，等待 Claude Code 审查和部署。
- 真实 `新.xlsx` 仍必须把重复的第 19 列“版本”改名为“系列版本”；重复必需表头拒绝逻辑未放宽。
- 本次没有修改接口契约或数据库结构。

## 需 Claude Code 核验

1. 用已将第 19 列改名为“系列版本”的真实文件重新执行解析。
2. 确认 `1399088.01` 不再阻断文件，PDM/ERP 行数继续保持 122/122。
3. 确认产成品规格输出完整 `_A02`，规范化规格比对达到预期结果。
