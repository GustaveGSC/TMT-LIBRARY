# 交接说明 · Claude → Codex：批准进入 D 批（只读方案先行）

日期：2026-07-26

## 本轮结论

resolve_all rename-table cutover 正式收口：全量重建正确性、原子发布、回滚路径、生产可用性均已
真实验证（两次独立全量重算691,990行结果一致，250,565个订单组件守恒逐单校验0不一致，回滚脚本
验证过一次真实使用）。完整记录见 `handoff/2026-07-26-claude-rename-cutover-closure.md`。

## 现在批准进入 D 批，但分两步，先只读方案，不直接上线施压

### 第一步：只读压测方案 + 验收阈值（现在开始）

不在生产直接施压。先输出：

1. **场景矩阵**（沿用原始方案 `handoff/2026-07-24-codex-task-cancellation-determinism-loadtest-plan.md`
   第五节，可按当前实际情况调整）：
   - 导入：1千/1万/5万/10万行，全新/完全重复/10%变化的财务重导；
   - 重算：1千/1万/10万/全量订单，shipping/finance/两来源合计；
   - 各阶段取消（parsing/inserting/resolving/committing）；
   - 并发场景（两客户端同时启动，验证409接管）。
2. **压测环境方案**：与生产同规格（2核/1.7GB/单sync worker/QueuePool 5+5）具体怎么落地——是用
   生产脱敏快照复制一份到独立实例，还是其他方式；明确不能直接在生产库上跑写压测。
3. **采集指标清单**：沿用原方案第五节"采集指标"，结合这次真实门禁已经验证过的方法论（比如组件
   守恒逐单校验，而不是简单聚合求和）。
4. **验收阈值**：基于这次真实生产数据（392秒完成692,003行全量重算、health p95<200ms、chart-data
   p95<2.5s）作为基线参考，提出具体的通过/失败判定线，不能只用"感觉快不快"。

这一步产出一份方案文档，不改代码、不跑压测，我复核后再进入第二步。

### 第二步：resolve_stale 10000 单临时上限校准

`MAX_STALE_RESOLVE_ORDERS=10000` 目前是未经真实压测验证的临时防护值（生产当前 is_stale=0，
从未在有真实 stale 数据时验证过安全边界）。需要：

1. 只读评估生产历史上 `is_stale=1` 的订单量级分布（比如查询历史上单次通用件/组合规则变更后
   实际产生的 stale 订单数量级，如果没有历史数据可查，说明这一点，不要编造）；
   仅仅统计生产数据，不修改产emean，不需要真的达到全库范围；
2. 结合 subset cutover（`DELETE ... WHERE EXISTS(...)`）的实际执行特征，评估 10000 单量级下是否
   会重演当初全表 DELETE 的 read_timeout 问题（这次是有 WHERE 条件的删除，理论上代价小很多，但
   需要用实际数据规模估算，不能只凭直觉）；
3. 给出建议：维持10000、调高、调低，或改成动态计算（比如按行数估算耗时的经验公式），并说明依据。

这一步也是只读评估 + 方案输出，不直接修改 `MAX_STALE_RESOLVE_ORDERS` 的值，除非方案确认后单独
授权。

## 边界

- 两项工作都不影响当前已上线的 resolve_all/取消能力，不需要暂停或降级现有功能；
- 不在生产库做任何写压测；
- 方案输出后，我会复核，你我确认后再决定是否需要真实压测环境、由谁执行、什么时候执行。

现在可以开始，不需要再等我确认。
