# 方案 v3：型号 bug 修复 + 原因判定规则 + 忽略规则（据用户 Excel 标注整理）

日期：2026-07-30
状态：**规则已整理，型号部分已修复并实测；等用户确认后再交 Codex**
来源：用户在 `2026年3月售后工单_AI预标注回测_待审核.xlsx` 的 42 条标注 + 三点共性反馈

## 一、型号：三个 bug 已修复，实测两个月份一致提升

| | 型号层 | 系列层 |
|---|---:|---:|
| 2026-03（样本外）修复前 | 67.8% | 88.7% |
| **2026-03 修复后** | **72.5%** | **92.8%** |
| 2026-02（调参月）修复前 | 70.1% | 85.4% |
| **2026-02 修复后** | **74.4%** | **90.2%** |

两个月同向提升，说明是真修复而非过拟合。已改入 `backend/scripts/aftersale/model_matcher.py`。

### bug 1（蜻蜓，最严重）：字典里括号全半角混用

字典中系列名是 `蜻蜓 (V1.2）`——**半角 `(` 配全角 `）`**。原正则只认全角，
版本号没被剥离，模型侧系列名 `蜻蜓 (V1.2）` 与备注抽出的 `蜻蜓` 对不上 → 收窄为空 →
按兜底逻辑忽略该参数 → 候选保持 255 个 → 掉回全局最高频（于是变成 DR11/JQ31）。

修复：四种括号组合都认。修复后蜻蜓全部正确落到 `QT12-*`，候选收窄到 1–2 个。
同类脏数据还有 `书架 (V1.1)领航员夹式本色`。

### bug 2（领航）：录入简称未对齐

`领航S单桌螺丝…` 原本一个参数都抽不到（词表只有 `领航员S`）。已加系列别名表：
`领航→领航员`、`领航S→领航员S`、`领航Z→领航员Z`、`领航A→领航员A`（长写法优先匹配）。

### bug 3：版本号吃掉了尺寸

`领航员PRO1.2米` 里的 `1.2` 被当成版本号。已排除紧跟 `米/m` 的数字。

### 用户已确认的 4 项，均已实现并验证

| 项 | 用户答复 | 实现 | 验证 |
|---|---|---|---|
| PRO 对应关系 | 对应领航员（V2.1），**产品标签的「别名」分类就是维护这个关系的** | 改为从「别名」标签读取（权威来源），不再硬编码销售名 | `领航PRO` → LH21 ✓ |
| 大白灯实为领航员Z | 小概率，后续交代录入人员注意 | **不实现**（按用户意见） | — |
| 阳光款 vs 阳光版 | 只是写法不同 | 「款」归一为「版」+ 词表最长匹配 | `领航S阳光款` → LY11 领航员S阳光版 ✓ |
| 大小写 | 忽略大小写 | 全流程 case-insensitive | `领航s` → 领航员S ✓ |

### 别名来源改造（重要）

销售名别名不再硬编码，改为读取产品标签「别名」分类（category id=16，13 条，
业务侧维护，映射到**具体系列编码**，比按系列名匹配更精确）：

| 别名 | 系列 |
|---|---|
| 领航员Pro | LH21 领航员（V2.1）|
| 启明星Pro | QM21 |
| 探索家U | TS22 |
| 多乐桌 / 时光桌 / 共学桌 | DL11 德罗 / GD11 高迪 / SG11-12 E时光 |
| S3学习灯 / 云光吸顶灯 | DL11 大白二代Lite / YG11 |

代码里只保留**录入简称**（`领航→领航员` 等），因为这类是录入习惯、不在数据库里。
顺序上必须先展开简称再查别名——备注写「领航pro」，别名表里是「领航员Pro」，
不先展开就永远匹配不上。

> **如实说明**：单独引入别名表对准确率是中性的（±1~4 单，噪声范围）。
> 本轮系列层的提升主要来自「阳光版」最长匹配修复。保留别名表的理由是架构正确
> ——业务加新别名时无需改代码，而不是它当下提升了数字。

### 最终成绩（含全部修复）

| | 型号层 | 系列层 |
|---|---:|---:|
| 2026-03（样本外） | 72.2% | **92.8%** |
| 2026-02（调参月） | 74.4% | **92.1%** |
| 起点（Codex 第二轮逐单推理） | 19.4% | 25.7% |

两个月系列层均约 92%，跨月一致。

### 新发现，需你判断：领航员PRO 的型号归属

别名表明确 `领航员Pro → LH21`（V2.1），规则据此给 `LH21NJ120-NW-A`，
但有 **4 单人工标的是 `LH12NJ120-NW-A`**（LH12 = 领航员 V1.2）：

```
领航员PRO1.2米 嫌弃拼接色差    规则 LH21NJ120-NW-A  人工 LH12NJ120-NW-A
领航员PRO1.2米 桌腿螺丝拧不上   规则 LH21NJ120-NW-A  人工 LH12NJ120-NW-A
领航员PRO1.2米 反馈有划伤      规则 LH21NJ120-NW-A  人工 LH12NJ120-NW-A
```

要么这几单人工标错了（PRO 标到了 V1.2 的型号上），要么「领航员Pro」实际不只对应 LH21。
按你说的"人工在型号层可能被历史匹配干扰"，我倾向前者，但这需要你确认，我不自己定。

## 二、原因判定规则（从 42 条标注提炼，共 6 类）

### A. 严重性优先：一单多原因时取更严重的那个

| 规则 | 出处 |
|---|---|
| 实木开裂 > 实木色差 | 行44 |
| 破损 > 瑕疵（提到"小孔"即破损） | 行156 |
| 椅背/椅垫 的原因等级 > 椅轮 | 行132 |
| 插座烧了（排插失效）等级最高 | 行216 |

### B. 部件归属：子部件归入父部件，除非备注明确指向子部件

| 规则 | 出处 |
|---|---|
| 旋转托盘 属于 椅垫 → 归"椅垫失效"，除非明确说托盘坏 | 行153、163 |
| 电源线 属于 电源 → 归"电源失效" | 行154 |
| 椅套 是独立于学习椅的配件 → 明确说椅套勾丝就归"椅套破损"，不归"学习椅瑕疵" | 行169 |
| 不存在"升降托盘"这个部件 | 行171 |

### C. 词语 → 原因 的确定映射

| 备注出现 | 应判 | 出处 |
|---|---|---|
| 脏污 | 产品脏污 | 行14 |
| 活动桌面板 翘 / 不平 | 桌面板不平（与"实木变形"的界限就以此划定） | 行32 |
| 自动灭 / 开了就灭 | 学习灯不亮 | 行134 |
| 磨损 | 实木磕碰划伤 | 行172 |
| 螺钉弯曲 | 螺钉破损 | 行210 |
| 电流声 | 电流异响 | 行77 |
| 电线问题 | 电线破损 | 行185 |
| 椅轮少物料 / 无异响的椅轮问题 | 椅轮失效（功能问题，非异响） | 行179、184 |

### D. 词典需合并的项

- `大白护套破损` 与 `大白塑料件破损` **合并为「大白塑料件破损」**（行220）
  —— **用户已在词典中合并完成**，无需我处理。

### E. 「没提到就不能判」——本轮 AI 最主要的错误来源

标注里有 6 条都是这一类：备注没提到某现象，AI 却判了它。

| 出处 | AI 判了 | 但备注没提到 |
|---|---|---|
| 行83 | 实木开裂 | 开裂 |
| 行206 | 桌面板不平 | 桌面板 |
| 行207 | 钣金掉漆 | 掉漆 |
| 行214 | 挡笔条破损 | 破损 |
| 行219 | 椅垫异响 | 异响 |
| 行179 | 椅轮异响 | 异响 |

**规则**：原因里的关键现象词（开裂/破损/异响/掉漆/不平…）必须在备注或留言里有对应
表述才能采用；否则退到该部件的通用"失效"类，或标"需人工判断"。这条应作为硬约束，
优先级高于相似案例检索——第一轮"高置信但错=0"说明模型有能力自控，缺的是这条明确禁令。

### F. 非产品问题的归类

| 情形 | 应判 | 出处 |
|---|---|---|
| 客户自身原因导致 | 客户原因 | 行160 |
| 不是产品问题、是发错物料 | 发错货 | 行183 |
| 二次售后（首次已处理，重寄后又出问题） | 二次售后 | 行180、182 |

## 三、忽略规则（v2：第三轮出问题后按用户修正并实测重定）

> **第三轮的教训**：我原先写的第 3 条「只写了寄什么物料、没写原因 → 忽略」是**有害的**，
> 直接造成忽略精确率从 100% 跌到 86.1%（12 单误判）。用户随后澄清：只有**纯**「补寄XXX」
> 才忽略；若备注还包含其他内容（如"明睿扭扭断，补寄2个"）就不能忽略。

### 判定分两类：机械可判的用代码，需语义的交模型

**A. 机械可判（建议直接用代码，精确率高）**

| 条件 | 判定 |
|---|---|
| 含「购买/已付/已支付/报价/自费/付款」**且无故障描述** | 忽略 |
| 含「补寄/补发/重新发」**且无故障描述**（纯补寄） | 忽略 |
| 含「补寄/补发」**但有故障描述** | **不忽略** |

**关键在于"故障描述"要识别得全。** 实测（2026-06，只含补寄类词的 80 单）：

| | 单数 | 人工判定 |
|---|---:|---|
| 有故障描述 + 补寄 | 71 | **88.7% 需处理** |
| 纯补寄无故障描述 | 9 | **77.8% 忽略** |

故障词表完整度直接决定成败：只用常见词时"纯补寄"桶里仅 53.8% 是忽略，
补上 `无法/不行/不能/不上/不下/不进/不住/相差/旧的/暗/紧/涩/不齐/高低/变色/起皮/
不满意/反馈/要求换/更换/换新/重拍` 之后升到 77.8%——**漏识别故障词会直接把需处理的单
误判为忽略**。

实测用过的完整故障词表：
```
断 坏 裂 破损 磕碰 划痕 划伤 不平 翘 异响 不亮 闪 失效 漏油 卡 松 掉 缺 没有 少
错 歪 偏差 生锈 掉漆 瑕疵 色差 脏 变形 不顺 拧不 装不 插不 滑丝 弯曲 异常 失灵
漏 响 抽不 关不 锁不 晃 凹 鼓 泡水 烧 霉 锈 污 痕 嫌弃 问题
无法 不行 不能 不上 不下 不进 不住 相差 旧的 暗 紧 涩 不齐 高低 变色 起皮
不满意 反馈 投诉 要求换 更换 换新 重拍
```

A 类整体实测：**精确率 96.3%（52/54，仅 2 单误判）、召回 44.4%**。
对比第三轮 Codex 全靠语义判断的 86.1% / 63.2%——精确率明显更高。

**B. 需语义判断（交给模型）**

- 备注与留言中**找不到任何可推断的问题**、表述含糊 → 忽略

A 类召回只有 44.4%，因为它只覆盖两种机械模式；117 个人工忽略单里其余的属 B 类。
**所以建议 A 类先用代码判、B 类交模型补召回**，而不是三条全丢给模型——
第三轮的教训正是全交语义判断时精确率会失守。

### 不可解的边界（2 单，可接受）

```
梦境抽屉橡胶圈补寄4个   → 人工：滑轨失效（需处理）
博睿补寄一个轮子        → 人工：椅轮缺失（需处理）
```

纯补寄、字面无故障，人工凭经验推断"缺件即故障"。这类无法从文本判定，按规则会误判为
忽略——占比极低（2/547），方向上是"少判需处理"，可接受。

**总原则不变：精确率优先**，不要为提召回牺牲它。

## 四、我建议的下一轮分工调整

第二轮已证明：型号匹配交给 LLM 逐单推理会塌缩（19.4%，只用 25 个唯一编码），
而确定性代码可达系列层 92.8%。所以：

- **型号**：由 `model_matcher.py` 出，Codex 不再逐单推理型号
- **原因/分类/简称/忽略判定**：Codex 负责，按本文第二、三节规则执行
- **安装问题/其他** 两类连续两轮 0% 召回：建议把这两类的全部历史案例备注
  （各 5–10 单，成本极低）直接给模型对齐口径

## 五、待确认事项汇总

1. 第一节表格里的 4 项型号规则（PRO 对应关系、大白灯、阳光款、大小写）
3. ~~补 Excel 两列~~ —— 用户已确认不用补
4. 上面新发现的「领航员PRO → LH21 还是 LH12」（4 单人工标注与别名表冲突）
5. 是否按第四节调整分工后开始第三轮
