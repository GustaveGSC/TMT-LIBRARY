# 2026-03 售后回测：Codex 修正规则盲标结果

日期：2026-07-30  
接收方：Claude Code  
状态：候选文件已生成，等待标准答案评分；未读取答案、未写入数据库。

## 产物

`tmp/holdout-202603/codex-predictions.json`

- 576 个唯一订单，全部覆盖；
- 严格使用约定 JSON 契约；
- 未读取 `holdout-202603-truth-*.json`。

## 本轮实施的修正

1. 型号候选从 `models-with-lifecycle.json` 过滤掉 `market='foreign'`，保留 `domestic`、`both` 与空值；输出中纯外贸型号数为 **0**。
2. 备注中先剔除购买日期，再抽取系列、尺寸、材质、颜色、升降方式等可见参数；日期只用于生命周期比对，未作为版本/尺寸候选。
3. 生命周期按备注中的购买年月筛选；无购买日期时不强行过滤。
4. 型号兜底按照新参考集计算的历史出现频次降序，再按尾缀和模型 id 稳定排序。
5. 安装、错漏件、钣金、外观四类采用交接文档规定的显式备注特征作为分类纠偏；购买/已付/报价类在没有故障证据时才保守建议忽略。
6. 物料、备注和不含持出月的已确认案例共同构成证据；词典只作交叉佐证，并非直接复用旧 `auto_match()`。

## 输出分布

| 项目 | 数量 |
|---|---:|
| 总订单 | 576 |
| 可优先审核 | 239 |
| 需人工判断 | 265 |
| 建议忽略 | 72 |
| 高置信 | 148 |
| 中置信 | 391 |
| 低置信 | 37 |

多物料或多段备注、但没有足够独立证据可安全拆分的订单均被保守标为“需人工判断”；本轮不为了提高原因行数量而伪造多原因行。

## 结构校验

- 576 条候选 / 576 个唯一订单号；
- 原因、分类、物料简称、型号均逐项存在于对应导出字典，越界值 0；
- 纯外贸型号 0；
- 标注阶段未读取任何答案文件。

## 请 Claude 后续执行

1. 用标准答案执行约定评分，型号能力以**系列层**为主指标，型号层仅作参考；
2. 单列“系列正确但型号不同”的订单，供用户抽查人工型号标注；
3. 分类别报告原因/分类召回，重点检查安装问题、其他、外观不良、钣金问题、错漏件；
4. 对建议忽略报告精确率与召回率，精确率优先；
5. 将预测、真值、评分和限制生成回测 Excel；不得写回售后数据。

