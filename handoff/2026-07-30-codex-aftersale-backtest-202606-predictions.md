# 2026-06 售后回测：Codex 原因/分类/简称盲标结果

日期：2026-07-30  
接收方：Claude Code  
状态：可评分；未读取标准答案，未写入生产数据。

## 产物

`tmp/holdout-202606/codex-predictions.json`

- 547 条，547 个唯一订单；
- 按本轮契约**不含 `model_code`**；型号请使用 `rule-model-suggestions.json` 的独立规则结果；
- 未读取 `holdout-202606-truth-cases.json` 或 `holdout-202606-truth-reasons.json`。

## 规则落实

1. 原因/分类/简称由备注、物料与不含 2026-06 的历史案例综合推断；不复用持出月数据。
2. “购买/已付/报价且无故障”、“仅补寄物料且无原因”单独建议忽略；精确率优先。
3. 对实木开裂、实木磕碰划伤、实木色差、桌面板不平、钣金掉漆、各类异响等，若备注/留言不存在必要现象词，则撤销具体原因建议，改为低置信人工判断；相似案例不得覆盖这一硬约束。
4. 安装问题、错漏件、钣金、外观的显式特征已按 v3 口径参与分类；“其他”校准样本仅用于理解口径，不作为答案来源。
5. 多物料但缺少可安全分离的独立问题证据时保守保留一条“需人工判断”，不伪造多原因行。

## 输出分布

| 项目 | 数量 |
|---|---:|
| 总订单 | 547 |
| 可优先审核 | 181 |
| 需人工判断 | 280 |
| 建议忽略 | 86 |
| 高置信 | 97 |
| 中置信 | 368 |
| 低置信 | 82 |

## 校验

- 原因、分类、物料简称均存在于本轮导出字典，越界值 0；
- 订单号唯一性通过；
- `model_code` 字段不存在，避免混淆规则模型匹配与 Codex 语义判断；
- 未读取真值文件。

## 请 Claude 评分

1. 合并 `rule-model-suggestions.json` 时明确标注型号成绩属于规则组件，不归因 Codex。
2. 评分重点：忽略精确率/召回、尾部分类（安装问题/其他）、“没提到就不能判”对具体原因误判的影响。
3. 按既定口径输出系列层/型号层，并单列系列正确但型号不同的人工标注复核清单。
4. 不达门槛前仅生成回测 Excel，绝不写回售后数据。

