# 2026-02 售后回测：Codex 盲标结果交接

日期：2026-07-29  
接收方：Claude Code  
状态：候选结果已生成，待评分与生成桌面 Excel；未写入任何生产数据。

## 输入与防泄漏确认

本轮仅读取：

- `tmp/backtest-202602/holdout-202602-blind.json`（217 个盲标订单）；
- `reference-confirmed-excl-202602.json`（不含 2026-02 的 1,865 条已确认原因行）；
- 原因、分类、物料简称、型号四份字典。

**未读取** `holdout-202602-truth-cases.json` 或 `holdout-202602-truth-reasons.json`。

## 产物

`tmp/backtest-202602/codex-predictions.json`

- 217 条，一订单一条初步建议；
- 严格符合上一份交接规定的 JSON 字段契约；
- 不含数据库写入动作，也不包含标准答案字段。

## 生成方法

这不是直接调用现有 `auto_match()`：

1. 对每个盲标订单，综合商家备注、物料编码/名称，检索不含持出月的人工确认案例；
2. 汇总相似案例的原因/型号/物料简称证据，并用当前字典中的原因关键词做交叉佐证；
3. “购买、已付、报价”且没有故障/缺件/破损描述的请求，单独标为“建议忽略”；
4. 多物料、多段备注、近邻标签冲突、或证据弱的订单降为“需人工判断”，不强行输出高置信结论。

现有关键词与简称库只被用作可解释证据之一；标签均来自字典，未生成新原因或新型号。

## 输出分布

| 项目 | 数量 |
|---|---:|
| 总订单 | 217 |
| 可优先审核 | 67 |
| 需人工判断 | 113 |
| 建议忽略 | 37 |
| 高置信 | 45 |
| 中置信 | 90 |
| 低置信 | 82 |

## 结构校验

- 217 条候选对应 217 个唯一订单号；
- 原因、一级分类、型号、物料简称均逐项存在于四份导出字典：无越界值；
- `suggested_action` 和 `confidence` 均为约定枚举值；
- 未读取标准答案文件。

## 请 Claude 后续执行

1. 读取标准答案，按既定维度评分：忽略判定、一级分类、具体原因、型号、物料简称；原因/分类须同时报告分组召回，避免头部类别掩盖问题。
2. 抽取“高置信但错误”和“低置信但正确”的代表案例，定位是备注理解、物料映射、多原因拆分还是忽略判定问题。
3. 生成桌面回测 Excel，并明确标注本轮仅为离线盲标、没有写回数据库。
4. 评分结果回交 Codex；在未达到预先约定门槛前，不得设计自动批量确认。

