# 交接说明 · Claude → Codex（第二十一轮，确认采用权限体系重做方案，两点修正）

日期：2026-07-23

`handoff/2026-07-23-codex-permission-system-redesign-proposal.md` 已读完，用户和我都认可你的方案质量明显高于我的初版：admin 全权代码绕过导致权限表不可审计、author 用户名硬编码是在扩大历史特例而不是收敛、用户/角色管理继续 admin-only 和"属于管理者"的要求本身矛盾——这三点批评都成立。**确认采用本文档方案作为实施基线，不按我原方案编码。**

两点修正：

## 1. 运维域去掉"版本发布"

我查了 `backend/routes/version/__init__.py`：这个蓝图是给 Electron 桌面端分发安装包用的（`POST /` 建版本记录、`POST /presign`+`POST /upload` 走 OSS 预签名上传安装包，GET 类给客户端查最新版本）。项目已经正式停止 Electron 支持（`handoff/2026-07-21-electron-support-end-decision.md`）："不制作/发布新安装包"，这个功能现在是冻结状态，不是"运维域里一个还在用的现有功能"。

请把方案里的 `ops:release:edit` 权限码和"版本发布相关写接口 → `ops:release:edit`"这条端点映射都去掉。`version` 蓝图维持现状（继续 admin-only），这次权限体系重做不动它——它本质上是待清理的死功能，属于另一个话题（之前全项目复查报告里"清理 Electron 死分支"那一条），不要混进这批。

运维域这次实际范围收窄为只有 `ops:login-config:edit` 一项，如果你觉得"运维"这个域名只对应一个权限码显得单薄，可以保留域名不变，也可以把域名判断先记在文档里、等以后真有其他运维类需求时再补权限码，不需要为了凑数现在生造功能。

## 2. 其余方案原样确认

- 四个权限域、`developer:analytics:view`/`account:users:view/edit`/`account:roles:view/edit`/`ops:login-config:edit`、三个标准角色（developer/manager/ops）、legacy admin 显式绑定全部标准权限并删除代码里的全局绕过、author 显式分配 developer 角色（不再靠用户名判断）、四批实施顺序、自动化测试最低要求——全部按你的文档执行，不需要再等我确认细节。
- "只读核实生产 admin/author 当前角色后，显式为 author 添加 developer；不盲目撤销其他角色"这条你已经想到了，很关键，照做。

## 顺带记一笔（关于协作方式，不是这次任务内容）

用户明确要求：以后交接说明都要按你这份方案文档的清晰程度来写（结构化对照表、明确的域名+权限码+端点映射、分批实施顺序、每批测试要求都摆出来）。我这边后续写给你的交接文档也会按这个标准执行，你写给我的也保持现在这个水平就行，不用因为这条反馈改变什么。

## 下一步

按你方案里的第一批（数据库与兼容映射）开始，去掉版本发布那部分后直接推进即可，不需要再等我这边二次确认。
